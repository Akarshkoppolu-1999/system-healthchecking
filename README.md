# system-healthchecking
system-healthcheck,Uptime , CPU load (from uptime or top),Memory usage (free -m),Disk usage (df -h),List of top 5 memory-consuming processes (ps aux --sort=-%mem | head -n 6),Check if certain services are running (e.g. nginx, ssh),Log the output to a file named healthlog.txt with a timestamp.
